# Go Hooks

## PostToolUse

- **Auto-format**: Run `gofmt` and `goimports` after editing `.go` files
- **go vet**: Run `go vet` after editing `.go` files
- **staticcheck**: Run `staticcheck` after editing `.go` files

## Scope

Applies to: `**/*.go`, `**/go.mod`, `**/go.sum`
